# 技术方案实现交付模板

## 改动摘要

| 文件 | 操作 | 改动说明 |
|---|---|---|
| {path} | 新增 / 修改 / 删除 | {summary} |

## 实现步骤完成情况

| 步骤 | 状态 | 说明 |
|---|---|---|
| Step 1：接口定义 | 完成 / 部分完成 / 未完成 | {notes} |
| Step 2：核心逻辑实现 | 完成 / 部分完成 / 未完成 | {notes} |
| Step 3：集成到现有代码 | 完成 / 部分完成 / 未完成 | {notes} |
| Step 4：验证 | 完成 / 部分完成 / 未完成 | {notes} |

## 接口检查点

- 新增接口 / 类 / 函数：{list}
- 与现有代码衔接方式：{integration}
- 发现的依赖、冲突或遗漏：{notes}

## 与技术方案的偏差

- {偏差 1：原因与影响}
- {偏差 2：原因与影响}

> 若无偏差，写“无明显偏差”。

## 验证结果

| 验证项 | 命令 / 方法 | 结果 |
|---|---|---|
| 基线检查 | `{command}` | 通过 / 失败 / 未运行：{reason} |
| 手动验证 | `{command or demo}` | 通过 / 失败 / 未运行：{reason} |
| 单元测试 | `{command}` | 通过 / 失败 / 未运行：{reason} |
| 格式 / 类型检查 | `{command}` | 通过 / 失败 / 未运行：{reason} |

## 覆盖的测试场景

- 正常路径：{test file or description}
- 边界情况：{test file or description}
- 兼容性：{test file or description}

## 剩余风险

- {risk 1}
- {risk 2}

## Git 提交计划

### commit 1

`{type}({scope}): {description}`

改动文件：

- {path}

改动原因：

{reason}

### commit 2

`{type}({scope}): {description}`

改动文件：

- {path}

改动原因：

{reason}

### commit 3

`{type}({scope}): {description}`

改动文件：

- {path}

改动原因：

{reason}

### commit 4

`{type}({scope}): {description}`

改动文件：

- {path}

改动原因：

{reason}

