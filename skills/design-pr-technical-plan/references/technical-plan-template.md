# 技术方案设计模板

## 技术方案设计

### 上下文

- 项目地址：{REPO_URL}
- 本地仓库 / fork：{LOCAL_PATH_OR_FORK}
- 项目核心架构：{一句话描述，例如“基于 ReAct 的单 Agent 循环，无显式规划层”}
- 选定贡献方向：{例如“引入 Human-in-the-loop 的 checkpoint 机制”}
- 涉及核心文件：{例如 agent/executor.py、tools/base.py}
- 当前实现缺陷：{来自 PR 分析报告的“缺陷 / 缺失”字段}

### 代码复核结论

| 文件 / 函数 | 当前行为 | 与缺陷的关系 | 证据 |
|---|---|---|---|
| {path:function} | {behavior} | {relation} | {line or snippet summary} |

> 若某个分析结论无法在代码中确认，要写明“未确认”，并说明还需要检查什么。

## 问题边界定义

**要解决的核心问题：** {一句话说明本 PR 要解决的唯一核心问题}

**不在本次 PR 范围内的问题：**

- {范围外事项 1}
- {范围外事项 2}
- {范围外事项 3}

**成功标准：**

- {可验证标准 1}
- {可验证标准 2}
- {可验证标准 3}

**约束条件：**

- 不破坏现有公共 API 的调用方式。
- 兼容项目当前支持的 Python / Node / 浏览器版本。
- 单个 PR 的改动尽量控制在 300 行以内，必要时拆分。
- 新增抽象必须有明确调用方，避免只为未来扩展而设计。

## 方案设计

### 方案 1：{方案名称}

**核心思路：** {2-3 句话描述设计思想}

**关键设计决策：**

- 新增 / 修改的核心抽象：{class / interface / function / hook / schema}
- 数据流：{输入 -> 处理 -> 输出}
- 集成点：{接入现有模块的位置}

**伪代码或接口签名草稿：**

```text
{根据项目语言替换为 Python / TypeScript / 其他语言，只展示关键接口，不写完整实现}
```

**优点：** {优势}

**缺点 / 风险：** {风险}

**适用场景：** {适合这个方案的前提}

### 方案 2：{方案名称}

**核心思路：** {2-3 句话描述设计思想}

**关键设计决策：**

- 新增 / 修改的核心抽象：{class / interface / function / hook / schema}
- 数据流：{输入 -> 处理 -> 输出}
- 集成点：{接入现有模块的位置}

**伪代码或接口签名草稿：**

```text
{根据项目语言替换为 Python / TypeScript / 其他语言，只展示关键接口，不写完整实现}
```

**优点：** {优势}

**缺点 / 风险：** {风险}

**适用场景：** {适合这个方案的前提}

### 方案 3：{方案名称}

**核心思路：** {2-3 句话描述设计思想}

**关键设计决策：**

- 新增 / 修改的核心抽象：{class / interface / function / hook / schema}
- 数据流：{输入 -> 处理 -> 输出}
- 集成点：{接入现有模块的位置}

**伪代码或接口签名草稿：**

```text
{根据项目语言替换为 Python / TypeScript / 其他语言，只展示关键接口，不写完整实现}
```

**优点：** {优势}

**缺点 / 风险：** {风险}

**适用场景：** {适合这个方案的前提}

## 方案对比矩阵

| 维度 | 方案 1 | 方案 2 | 方案 3 |
|---|---|---|---|
| 实现复杂度 | {低/中/高} | {低/中/高} | {低/中/高} |
| 对现有代码侵入性 | {低/中/高} | {低/中/高} | {低/中/高} |
| 可扩展性 | {低/中/高} | {低/中/高} | {低/中/高} |
| 预计 PR 通过难度 | {低/中/高} | {低/中/高} | {低/中/高} |
| 面试叙事价值 | {低/中/高} | {低/中/高} | {低/中/高} |

**推荐方案：** 方案 {N}

**推荐理由：** {结合矩阵，说明为什么这个方案最适合作为开源贡献}

## 实现规划

### 需要新增的文件

| 文件路径 | 职责 | 预计行数 |
|---|---|---|
| {path} | {responsibility} | {lines} |

### 需要修改的文件

| 文件路径 | 修改内容 | 改动幅度 |
|---|---|---|
| {path} | {change} | 小 / 中 / 大 |

### 实现步骤

1. {具体任务，例如“在 base.py 中定义 CheckpointManager 接口”，完成后项目仍可运行}
2. {具体任务，完成后项目仍可运行}
3. {具体任务，完成后项目仍可运行}
4. {具体任务，完成后项目仍可运行}

### 验证策略

- **手动验证：** {如何跑通端到端 demo}
- **单元测试：** {至少 2-3 个核心测试用例}
- **边界情况：** {异常、空输入、失败恢复、兼容性、回归风险}

## 与维护者沟通前的准备

{英文评论草稿，不超过 150 字。内容包含：发现的问题、提议方向、询问是否接受，以及是否已有计划。}
